import styled from 'styled-components';

const StyledButton = styled.button`
  font-style: normal;
  font-weight: 500;
  font-size: 20px;
  padding-top: 10px;
  padding-bottom: 10px ;
  border-radius: 16px;
  color: #F0F4EF;
`;

export default StyledButton;
