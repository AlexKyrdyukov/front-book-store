import styled from 'styled-components';

const StyledBooksLists = styled.section`
  display: grid;
  grid-template-columns: repeat(4, auto);
  grid-template-rows:repeat(4, auto) 
`;

export default StyledBooksLists;
