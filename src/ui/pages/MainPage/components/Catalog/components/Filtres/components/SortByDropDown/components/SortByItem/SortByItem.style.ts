import styled from 'styled-components';

const StyledSortByItem = styled.div`

  .sort-by__text {
    font-weight: 500;
    font-size: 16px;
    padding-top: 12px;
    padding-bottom: 12px;
    padding-left: 15px;
    color: #B9BAC4;
  }
  
  .active {
    color: #344966;
  }
  
  .disable {
  }
`;

export default StyledSortByItem;
